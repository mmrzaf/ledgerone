# Flutter Starter Template (pre-0.1)

Purpose: skeleton & contracts for Onboarding → Login → Home with email/password auth.

Structure: app/ core/ features/ shared/ assets/ docs/ tool/

Quickstart:
1. Install Flutter stable.
2. Run `tool/bootstrap`.
3. Run `flutter run` to start the app.
