const Map<String, dynamic> defaultFlags = {
  // Feature Gates
  'auth.enabled': true,
  'onboarding.enabled': true,
  'home.promo_banner.enabled': false, // Hidden by default
  // System
  'telemetry.enabled': true,
  'retry.max_attempts': 3,

  // UI
  'ui.theme_variant': 'default',
};
